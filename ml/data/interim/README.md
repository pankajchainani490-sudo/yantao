# Interim Data

This directory stores parsed flows, cleaned packet tables, and intermediate CSV files.

Typical outputs for this stage:

- pcap to flow conversion results
- per-window traffic summaries
- partially cleaned records before final feature selection
